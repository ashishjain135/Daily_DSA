reverse entire array

class Solution {
	public void reverseArray(int arr[]) {
	// code here
		int n = arr.length;
			int l = 0;
			int r = n-1;
			while(l<r){
				int temp = arr[l];
				arr[l] = arr[r];
				arr[r] = temp;
				l++;
				r--;
			}
		}
}
